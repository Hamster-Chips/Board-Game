#include "player.h"
#include <iostream>
#include <fstream>

using namespace std; 

Player::Player() : name(""), score(0) {}

Player::Player(const string& name, int score, const std::string& playerItems)

Player::~Player() {}